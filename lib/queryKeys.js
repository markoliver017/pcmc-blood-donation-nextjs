export const queryKeys = {
    adminDashboard: ["admin-dashboard-data"],
    // Add other query keys here as the application grows
    // e.g., allEvents: ['all-events'],
    // e.g., eventById: (id) => ['event', id],
};
